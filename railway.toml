[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx turbo build"

[deploy]
startCommand = "cd apps/web && npm run start -- -p ${PORT:-3000}"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
